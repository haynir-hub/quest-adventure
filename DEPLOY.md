# Quest Adventure - מקור אחד, יעד אחד

נכתב 24.8.2026 אחרי שהתברר שעבודה נכונה לא הגיעה לאוויר במשך ימים, ואף אחד
לא ידע.

## מה קרה, כדי שלא יקרה שוב

עבודה על המשחק נעשתה בעותקים זמניים על השרת הביתי, תחת
`~/.cache/hn-codex-tmp/quest-*`. הקומיטים נכנסו לשם, **התיקיות נמחקו**,
והפלט מעולם לא הגיע לא לגיטהאב ולא לאתר החי. ענף שלם עם מנגנון סרטוני
הדמויות כמעט אבד ככה.

במקביל התברר ש**ששת סרטוני הדמויות מעולם לא היו על האתר החי בכלל.** לא היה
שם אפילו תיקיית `videos/`. הפיצ'ר לא נשבר, הוא פשוט אף פעם לא עלה.

## מקור האמת. אין אחר

| מה           | איפה                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------ |
| הריפו היחיד  | `/Volumes/Nir_Crucial/01-Dev_Projects/04_Done/WebApps _by_HayNir/quest-adventure` (על המק, על הכונן החיצוני) |
| הריחוק היחיד | `git@github.com:haynir-hub/quest-adventure.git`                                                              |
| האתר החי     | `root@140.82.32.36:/srv/quest-adventure/`                                                                    |
| הכתובת       | `https://140-82-32-36.sslip.io/quest/`                                                                       |

**אסור לעבוד על עותק זמני.** אם המכונה שאתה רץ עליה לא רואה את הנתיב שלמעלה,
זו הסיבה לעצור ולדווח, לא לשכפל לתיקייה זמנית.

**עבודה שלא נדחפה לגיטהאב היא עבודה שלא קיימת.**

## דיפלוי

`/srv/quest-adventure/` היא תיקיית תוצרי בילד בלבד, **לא ריפו**. nginx עושה
alias מ-`/quest/` לשם.

```bash
npm run build                                   # base חייב להיות /quest/
rsync -a --delete dist/ root@140.82.32.36:/srv/quest-adventure/
ssh root@140.82.32.36 deploy quest-adventure    # מגבה בלבד. הוא לא מושך קבצים
```

`deploy` **רק יוצר גיבוי**. הוא לא מוריד כלום משום מקום. אם לא העלית, לא
פרסת.

## המלכודת שהסתירה הכל, ואיך בודקים נכון

הגדרת nginx היא `try_files $uri $uri/ /quest/index.html`. כלומר **כל כתובת
שלא קיימת מחזירה את דף הבית עם קוד 200.** בדיקה לפי קוד תגובה תמיד תגיד
"הצלחה", גם כשהקובץ לא קיים.

```bash
curl -s -o /dev/null -w '%{http_code} %{size_download} %{content_type}\n' \
  https://140-82-32-36.sslip.io/quest/videos/doctor-nowhere/filbus.mp4
```

- **תקין:** `200`, מיליוני בתים, `video/mp4`
- **הקובץ לא שם:** `200`, כ-666 בתים, `text/html`

666 בתים זה בדיוק הגודל של `index.html`. **בודקים גודל וסוג תוכן, לא קוד
תגובה.**
